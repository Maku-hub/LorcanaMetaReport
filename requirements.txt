# Dependencies live in pyproject.toml; this file just points at them so
# `pip install -r requirements.txt` and `pip install -e .` can never drift apart.
# Two lists of the same packages is how one of them goes stale.
-e .
